# --- Secrets --------------------------------------------------------------
# Machine-local overrides and anything holding credentials live outside the
# repo; these patterns are a backstop in case one lands inside it by mistake.
*.local
*.local.zsh
*.secrets.zsh
secrets.zsh
*.env
.env
.env.*

# --- OS / editor ----------------------------------------------------------
.DS_Store
.AppleDouble
*.swp
*.swo
*~

# --- Other ----------------------------------------------------------------
notes/
