# Shell behavior options.
setopt AUTO_CD               # `foo` with no command cd's into ./foo
setopt EXTENDED_GLOB         # richer globbing (^, #, ~ operators)
setopt GLOB_DOTS             # let globs match dotfiles
setopt INTERACTIVE_COMMENTS  # allow # comments at an interactive prompt
setopt NO_BEEP               # stop the terminal bell
