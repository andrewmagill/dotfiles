# Debian/Ubuntu packages (used by the WSL2 box) — one per line.
# Installed with: sudo apt-get install -y $(grep -vE '^\s*(#|$)' packages/apt.txt)
# Note: on Debian/Ubuntu, `fd` is the `fd-find` package (binary: fdfind).
zsh
# neovim is installed from the official prebuilt release by bootstrap.sh
# (distro packages are too old for a modern LSP config) — see install_neovim().
git
stow
ripgrep
fd-find
# bat (binary: batcat on Debian/Ubuntu) + git-delta (binary: delta): highlighters
bat
git-delta
fzf
curl
# unzip: general archive extraction utility
unzip
# gcc + make: compile nvim-treesitter parsers and telescope-fzf-native
gcc
make
# shell dev tooling: shellcheck (pre-commit lint) + bats (test runner)
shellcheck
bats
# podman: rootless, daemonless container engine
podman
