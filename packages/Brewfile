# macOS packages — installed with: brew bundle --file=packages/Brewfile
brew "zsh"
brew "neovim"
brew "git"
brew "stow"
brew "ripgrep"
brew "fd"
brew "bat"
brew "git-delta"
brew "fzf"
brew "curl"
brew "starship"
brew "mise"

# Terminal emulator (uses the config in common/.config/kitty/)
cask "kitty"

# Nerd Font (adds glyphs for the prompt). Select it in your terminal afterward.
# OpenDyslexicM Nerd Font Mono — matches Alacritty / WezTerm / Windows Terminal.
cask "font-opendyslexic-nerd-font"
